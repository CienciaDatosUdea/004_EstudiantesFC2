#include "include/flow.h"

int main() {
    FlowManager app;                   // Se crea un objeto de la clase FlowManager
    app.run();                         // Se ejecuta el flujo principal de la aplicación
    return 0;
}
