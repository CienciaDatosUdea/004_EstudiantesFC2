#include "fir_filter.h"

// Clase para manejar el flujo de datos
class FlowManager {
public:
    FlowManager();   // Constructor
    void run();      // Ejecuta el flujo de diseño del filtro
};

